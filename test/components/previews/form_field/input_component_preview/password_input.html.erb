<%= form_with url: "#", scope: :preview do |form| %>
  <%= render(FormField::InputComponent.new(form: form, field: field, label: label, type: type, placeholder: "Enter #{label.downcase}")) %>
<% end %>
